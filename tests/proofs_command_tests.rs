#[cfg(test)]
mod tests {

    #[test]
    fn test_proofs_command_with_default_file() {
        // TODO: fix the test
        // execute_preliminary_commands();
        // create_addresses_file("addresses.txt");

        // let mut cmd = Command::cargo_bin(env!("CARGO_PKG_NAME")).unwrap();
        // cmd.arg("proofs").assert().success();
    }

    #[test]
    fn test_proofs_command_with_specified_file() {
        // TODO: fix the test
        // execute_preliminary_commands();
        // let file_name = "test_addresses.txt";
        // create_addresses_file(file_name);

        // let mut cmd = Command::cargo_bin(env!("CARGO_PKG_NAME")).unwrap();
        // cmd.args(&["proofs", file_name]).assert().success();
    }
}
